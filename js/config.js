// Minimal shared config for the start screen (extends later in game)
window.SF = {
  VERSION: '0.1.0',
  STORAGE_PREFIX: 'sf.v1.',
  // Convention: we’ll consider these keys as valid save roots
  SAVE_KEYS: ['player','profile.slot0','profile.slot1','profile.slot2','world','factions']
};